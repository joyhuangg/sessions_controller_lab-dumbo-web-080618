require 'rails_helper'

RSpec.describe "homepage", :type => :view do
  # it "redirects to login page if user not logged in" do
  #   visit '/'
  #   expect(response).to redirect_to controller: 'sessions', action: 'new'
  # end
  # it "redirects to hello page if user is logged in", :type => :view do
  #   visit '/'
  #   expect(response).to redirect_to controller: 'application', action: 'hello'
  # end
end
